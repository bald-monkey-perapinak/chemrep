# Database (shared with backend)
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# Browser automation (VCS integration)
playwright==1.44.0

# Async HTTP (ElevenLabs TTS, Miro API)
httpx==0.27.0

# Audio conversion (MP3→PCM для ElevenLabs)
pydub==0.25.1
# + системная зависимость: apt-get install ffmpeg

# Utils
python-dotenv==1.0.1

# S3/MinIO (для сохранения записей уроков)
boto3==1.35.0

# ── ASR (раскомментировать на сервере) ────────────────────────────────────
# faster-whisper==1.0.3
# webrtcvad==2.0.10
# numpy==1.26.4

# ── TTS локальный (альтернатива ElevenLabs) ───────────────────────────────
# torch==2.3.0          # CPU-сборка: pip install torch --index-url https://download.pytorch.org/whl/cpu
# torchaudio==2.3.0

# ── LLM ──────────────────────────────────────────────────────────────────
httpx==0.27.0       # уже в зависимостях (ElevenLabs), используется и для Claude API

# ── Miro (следующий этап) ─────────────────────────────────────────────────
# miro-api-python>=2.1.0
